// var r1= document.getElementById('rob4');      // Create a new element
//               // Add it to the page
//   function dismissNote() {                          // Declare function
//   document.body.removeChild(r1);              // Remove the note
// }
   // Get the close button
// Click close-clear note
function rem() {
    // Removes an element from the document
    var element = document.getElementById('rob4');
    var element1 = document.getElementById('rob3');
    element.remove();
    }
    function rem1() {
        // Removes an element from the document

        var element1 = document.getElementById('rob3');
        element1.remove();
        }
        function rem2() {
            // Removes an element from the document
            var element = document.getElementById('rob2');

            element.remove();
            }
